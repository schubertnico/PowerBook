name: CI

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: powerbook_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    strategy:
      fail-fast: false
      matrix:
        # PowerBook benötigt PHP 8.4 (siehe composer.json: "php": "^8.4").
        # Aeltere Versionen werden bewusst NICHT getestet, weil Features wie
        # property-hooks / asymmetrische Visibility / declare(strict_types=1)
        # mit 8.4-spezifischen Annotationen genutzt werden.
        php-version: ['8.4']

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup PHP ${{ matrix.php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          extensions: pdo, pdo_mysql, mbstring
          coverage: xdebug

      - name: Validate composer.json
        run: composer validate --strict

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v5
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ matrix.php-version }}-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            ${{ runner.os }}-php-${{ matrix.php-version }}-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Run PHPStan (Level 8)
        run: composer phpstan

      - name: Run Psalm (Level 4)
        run: composer psalm

      - name: Run PHPMD
        run: composer phpmd

      - name: Check code style (PHP-CS-Fixer)
        run: composer cs-check

      - name: Run PHPUnit tests
        run: composer test

      - name: Run PHPUnit with coverage
        if: matrix.php-version == '8.4'
        run: |
          composer test -- --coverage-text --coverage-clover=coverage.xml

      - name: Upload coverage to Codecov
        if: matrix.php-version == '8.4'
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
        continue-on-error: true

  code-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo, pdo_mysql, mbstring

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Check code style (PHPStan Level 8)
        run: composer phpstan

      - name: Run Psalm (Level 4)
        run: composer psalm

      - name: Check code quality (PHPMD)
        run: composer phpmd

      - name: Check code style (PHP-CS-Fixer)
        run: composer cs-check
